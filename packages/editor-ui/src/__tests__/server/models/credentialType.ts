import { Model } from 'miragejs';
import type { ModelDefinition } from 'miragejs/-types';
import type { ICredentialType } from 'bonkxbt-workflow';

export const CredentialTypeModel: ModelDefinition<ICredentialType> = Model.extend({});
